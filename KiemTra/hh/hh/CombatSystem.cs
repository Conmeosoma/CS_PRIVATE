namespace hh;

public class CombatSystem
{
    public Player player;
    public static void StartBattle(Player p, List<Enemy> enemyList)
    {
        enemyList.Clear();
        while (p!.Health > 0 && enemyList.Count > 0)
        {
            
        }
    }
}